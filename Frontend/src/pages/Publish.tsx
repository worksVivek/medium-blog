import MinimalEditor from "../Components/PublishComponent"; // Adjust the import path as needed.

export const Publish = () => {

  
  
  return (

    <div className="container mx-auto ">
      
      <MinimalEditor />
    </div>
  );
};

export default Publish;
